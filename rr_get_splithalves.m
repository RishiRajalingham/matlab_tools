function trial_splits = rr_get_splithalves(trials, splitvar)
% function trial_splits = rr_get_splithalves(trials, splitvar)

    trial_splits = cell(2,1);
    us = unique(splitvar);
    for si = 1:length(us) 
        tsi = find(splitvar == si);
        
    end

    


end